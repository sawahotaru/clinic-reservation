<?php
// ============================================================
//  初期管理アカウント設定
//  - ここに書いた値は「初回アクセス時」にDBへ登録されます（以降はDBの値を使用）。
//  - 「ログイン情報リセット」を実行すると、IDとパスワードがこの初期値に戻ります。
//  - 初期パスワードはDBには平文では保存されず、ハッシュ化されて保存されます。
//  ※ 設置後は、ここの初期値（特にパスワード）を必ず変更してください。
// ============================================================

// 初期ログインID（メールアドレス形式）
define('INITIAL_ADMIN_ID', 'admin@example.com');

// 初期パスワード
define('INITIAL_ADMIN_PASSWORD', 'changeme');

// 初期メールアドレス（パスワード再設定・ログインリセットのリンク送信先）
define('INITIAL_ADMIN_EMAIL', 'owner@example.com');

// 【緊急復旧】メールが使えずログインできない時の最終手段。
//   次の行のコメントを外す（先頭の // を消す）と、次回アクセス時に
//   ログインIDとパスワードが上の初期値に戻ります。
//   復旧できたら、必ずこの行を元のコメントに戻してください（戻さないと毎回初期化されます）。
// define('FORCE_INITIAL_ADMIN', true);

// ※ デモモード（実際には送信せず記録のみ）は、管理画面の「設定」から ON/OFF できます。
//   設定はDBに保存されます（このファイルでは指定しません）。
